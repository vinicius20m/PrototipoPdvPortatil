
const initialState = {
    list: [
        {title: 'Primeira Nota', body: 'Teste de primeira nota....'}
    ]
};

export default (state = initialState, action) => {

    switch(action.type) {
        case 'ADD_NOTE' :

        break;
    }

    return state;
}

